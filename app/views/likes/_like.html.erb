
<% if  user_signed_in? %>
  <% if current_user.already_liked?(@post) %>
     <%= link_to 'いいねを外す！', post_like_path(@post), method: :delete  %> 
  <% else  %>
    <%= link_to 'いいねをする！', post_likes_path(@post), method: :post  %> 
  <% end %>
<% end %>
<%= "いいね！#{@post.likes.count}" %>
